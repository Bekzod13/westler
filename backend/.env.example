DATABASE_URL="file:./dev.db"
JWT_SECRET=
JWT_EXPIRES_SEC=604800
PORT=3000
# Optional: absolute URLs in JSON for /uploads/... (e.g. public API & upload response). Defaults to relative paths if unset.
PUBLIC_BASE_URL=http://localhost:3000
ADMIN_ORIGIN=http://localhost:5173
FRONTEND_ORIGIN=http://localhost:3000
# Production (HTTPS, no trailing slashes). Comma-separate multiple origins if needed.
# ADMIN_ORIGIN=https://your-admin.netlify.app
# FRONTEND_ORIGIN=https://www.yourdomain.com
SEED_ADMIN_LOGIN=admin
SEED_ADMIN_PASSWORD=admin12345
